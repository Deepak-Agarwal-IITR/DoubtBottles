<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lecture.name %> </title>
</head>
<body>
    <%-include('../partials/navbar')%>
    <%-include('../partials/flash')%>
    <h1><%=lecture.name%> </h1>
    <% if(currentUser && currentUser._id.equals(course.teacher)){ %>
    <a href="/courses/<%=course._id%>/lectures/<%=lecture._id%>/edit">Edit</a>
    <form action="/courses/<%=course._id%>/lectures/<%=lecture._id%>?_method=DELETE" method="post">
        <button>Delete</button>
    </form>
    <%}%>
    <%if(currentUser){%>
    <a href="/courses/<%=course._id%>/lectures/<%= lecture._id %>/comments/new">Add a question</a>
    <%}%>

    <%- include('../partials/printComments',{comments:lecture.comments,lecture,id: course._id}) %> 
</body>
</html>